---
x-i18n:
    generated_at: "2026-04-06T03:05:50Z"
    model: gpt-5.4
    provider: openai
    source_hash: 6e1cf417b0c04d001bc494fbe03ac2fcb66866f759e21646dbfd1a9c3a968bff
    source_path: .i18n/README.md
    workflow: 15
---

# موارد تدويل مستندات OpenClaw

يخزن هذا المجلد إعدادات الترجمة لمستودع المستندات المصدر.

توجد الآن صفحات اللغات المُنشأة وذاكرة الترجمة الحية للغات في مستودع النشر (`openclaw/docs`، ونسخة السحب المحلية الشقيقة `~/Projects/openclaw-docs`).

## الملفات

- `glossary.<lang>.json` — تعيينات المصطلحات المفضلة (تُستخدم في إرشادات المطالبة).
- `<lang>.tm.jsonl` — ذاكرة الترجمة (ذاكرة تخزين مؤقت) مفهرسة حسب سير العمل + النموذج + تجزئة النص. في هذا المستودع، تُنشأ ملفات TM الخاصة باللغات عند الطلب.

## تنسيق المسرد

`glossary.<lang>.json` عبارة عن مصفوفة من الإدخالات:

```json
{
  "source": "troubleshooting",
  "target": "故障排除",
  "ignore_case": true,
  "whole_word": false
}
```

الحقول:

- `source`: العبارة الإنجليزية (أو عبارة المصدر) المطلوب تفضيلها.
- `target`: ناتج الترجمة المفضل.

## ملاحظات

- تُمرر إدخالات المسرد إلى النموذج باعتبارها **إرشادات للمطالبة** (من دون إعادات كتابة حتمية).
- لا يزال `scripts/docs-i18n` مسؤولًا عن إنشاء الترجمة.
- يزامن المستودع المصدر المستندات الإنجليزية إلى مستودع النشر؛ ويجري إنشاء اللغات هناك لكل لغة عند الدفع، والجدولة، وإرسال الإصدار.
