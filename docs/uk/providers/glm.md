---
read_when:
    - Ви хочете моделі GLM в OpenClaw
    - Вам потрібні схема іменування моделей і налаштування
summary: Огляд сімейства моделей GLM + як використовувати його в OpenClaw
title: Моделі GLM
x-i18n:
    generated_at: "2026-04-12T10:36:40Z"
    model: gpt-5.4
    provider: openai
    source_hash: 12469bda0298f05fd94dc76a3183ab0e08257ec12e84ad1ed393107880e193de
    source_path: providers/glm.md
    workflow: 15
---

# Моделі GLM

GLM — це **сімейство моделей** (а не компанія), доступне через платформу Z.AI. У OpenClaw
до моделей GLM звертаються через провайдера `zai` та ідентифікатори моделей на кшталт `zai/glm-5`.

## Початок роботи

<Steps>
  <Step title="Виберіть варіант автентифікації та запустіть онбординг">
    Виберіть варіант онбордингу, що відповідає вашому плану Z.AI та регіону:

    | Варіант автентифікації | Найкраще підходить для |
    | ---------------------- | ---------------------- |
    | `zai-api-key` | Загального налаштування через API-ключ з автоматичним визначенням кінцевої точки |
    | `zai-coding-global` | Користувачів плану Coding (глобально) |
    | `zai-coding-cn` | Користувачів плану Coding (регіон Китай) |
    | `zai-global` | Загального API (глобально) |
    | `zai-cn` | Загального API (регіон Китай) |

    ```bash
    # Приклад: загальне автоматичне визначення
    openclaw onboard --auth-choice zai-api-key

    # Приклад: глобальний план Coding
    openclaw onboard --auth-choice zai-coding-global
    ```

  </Step>
  <Step title="Установіть GLM як модель за замовчуванням">
    ```bash
    openclaw config set agents.defaults.model.primary "zai/glm-5.1"
    ```
  </Step>
  <Step title="Переконайтеся, що моделі доступні">
    ```bash
    openclaw models list --provider zai
    ```
  </Step>
</Steps>

## Приклад конфігурації

```json5
{
  env: { ZAI_API_KEY: "sk-..." },
  agents: { defaults: { model: { primary: "zai/glm-5.1" } } },
}
```

<Tip>
`zai-api-key` дає OpenClaw змогу визначити відповідну кінцеву точку Z.AI за ключем і
автоматично застосувати правильний базовий URL. Використовуйте явні регіональні варіанти, якщо
хочете примусово вибрати конкретний план Coding або загальну поверхню API.
</Tip>

## Вбудовані моделі GLM

Наразі OpenClaw додає до вбудованого провайдера `zai` такі посилання на GLM:

| Модель           | Модель            |
| ---------------- | ----------------- |
| `glm-5.1`       | `glm-4.7`        |
| `glm-5`         | `glm-4.7-flash`  |
| `glm-5-turbo`   | `glm-4.7-flashx` |
| `glm-5v-turbo`  | `glm-4.6`        |
| `glm-4.5`       | `glm-4.6v`       |
| `glm-4.5-air`   |                  |
| `glm-4.5-flash` |                  |
| `glm-4.5v`      |                  |

<Note>
Посиланням на вбудовану модель за замовчуванням є `zai/glm-5.1`. Версії GLM і їхня доступність
можуть змінюватися; перевіряйте документацію Z.AI, щоб отримати найактуальнішу інформацію.
</Note>

## Додаткові примітки

<AccordionGroup>
  <Accordion title="Автоматичне визначення кінцевої точки">
    Якщо ви використовуєте варіант автентифікації `zai-api-key`, OpenClaw аналізує формат ключа,
    щоб визначити правильний базовий URL Z.AI. Явні регіональні варіанти
    (`zai-coding-global`, `zai-coding-cn`, `zai-global`, `zai-cn`) вимикають
    автоматичне визначення та напряму фіксують кінцеву точку.
  </Accordion>

  <Accordion title="Деталі провайдера">
    Моделі GLM надаються runtime-провайдером `zai`. Повну інформацію про конфігурацію провайдера,
    регіональні кінцеві точки та додаткові можливості дивіться в
    [документації провайдера Z.AI](/uk/providers/zai).
  </Accordion>
</AccordionGroup>

## Пов’язане

<CardGroup cols={2}>
  <Card title="Провайдер Z.AI" href="/uk/providers/zai" icon="server">
    Повна конфігурація провайдера Z.AI та регіональні кінцеві точки.
  </Card>
  <Card title="Вибір моделі" href="/uk/concepts/model-providers" icon="layers">
    Вибір провайдерів, посилань на моделі та поведінки резервного перемикання.
  </Card>
</CardGroup>
